<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="member"> 
	<resultMap type="hbmvo" id="hbmvoRM">
 		<result property="memberEmail" column="member_email"/>
 		<result property="memberPassword" column="member_password"/>
 		<result property="memberName" column="member_name"/>
 		<result property="babyName" column="member_baby_name"/>
 		<result property="babyBirthDate" column="member_baby_birthday"/>
 		<result property="babySex" column="member_baby_sex"/>
 	</resultMap>
 	
 	<!-- 용호 작성 쿼리 -->
	<!-- 회원(+아이) 정보 삽입 -->
	<insert id="registMember" parameterType="hbmvo">
		insert into hangeulbot_member (member_email, member_password, member_name, 
		member_baby_name, member_baby_birthday, member_baby_sex)
		values (#{memberEmail}, #{memberPassword}, #{memberName}, #{babyName}, #{babyBirthDate}, #{babySex})
	</insert>
	<!-- 이메일과 일치하는 회원정보 select -->
	<select id="getMemberInfoByEmail" resultMap="hbmvoRM">
		select member_email, member_password, member_name, member_baby_name, member_baby_birthday, member_baby_sex 
		from hangeulbot_member
		where member_email = #{value}
	</select>
	
 </mapper>

 
 	
 
 
 
 
 
 
 
 
 
 
 
 
 
















